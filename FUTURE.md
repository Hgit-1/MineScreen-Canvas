# MineScreen Future Roadmap

`PORTING.md` contains historical compatibility notes for developers. This file covers MineScreen's
own future features and does not promise WebDisplays behavior parity.

本文档记录尚未进入当前稳定功能集的长期方向。条目代表设计预期，不承诺具体版本或发布日期。

## 新型展示板

### 电光展示板

- 提供低分辨率点阵、单色、双色与全彩 LED 外观，而不是把普通 Screen 纹理简单缩放。
- 支持像素间距、扫描方向、亮度、刷新率、跑马灯和故障像素等展示参数。
- 多人同步以内容、布局、动画参数和统一起始时间为主，不逐帧通过 Minecraft 服务器传图。

### 同步文字与动画文字展示板

- 当前 MVP 已提供独立的“同步文字展示板”和“动画文字展示板”：服务器校验并同步文字、前景/背景色、动画类型、速度与统一游戏时间起点。
- 静态换行、滚动和呼吸动画均由客户端按相同时间参数渲染，Minecraft 服务器不转发逐帧文字图片；放置者拥有编辑权，管理员可代为编辑。
- 当前版本还支持字号、同向相邻同类展示板自动连接，以及 `ALERT` 提醒闪烁动画；连接分量由确定性的主方块统一渲染，缺失瓦片不生成多余画面。
- 后续继续加入多页面、字体包、语言变量、缺字回退和更严格的跨客户端自动排版一致性。

### HTML 直接展示板

- 面向受控模板的 HTML/CSS 展示，不等同于当前具备完整导航能力的 WEB 浏览器。
- 支持离线模板缓存、CSP、资源白名单和分级脚本权限，适合低开销的公共信息界面。
- 将模板、结构化数据和动画时钟分开同步；客户端本地排版，服务器无需转发视频帧。

### 异型与交通显示屏

- 支持细长、分段、非矩形及后续可选曲面网格，不再把所有设备限制为方块矩形画布。
- 规划类似 S-TRAIN 车厢 `Smileビジョン` 的车门上方/车厢信息屏：线路、站名、换乘、广告和紧急信息可分区显示。
- 异型设备使用明确的有效像素遮罩、UV 岛和点击区域，空洞不会生成黑色单面几何。

### 交通显示屏外部模板包

- 当前已提供可用的交通显示屏方块和内置结构化模板，支持线路、目的地、当前站、下一站、ETA、状态与模板 ID；客户端外部模板预计放在 `config/minescreen/templates/<template_id>/`，包含 `manifest.json`、`layout.json` 与 `assets/`。
- 模板负责布局、字体、颜色、图标和变量绑定；默认禁止任意脚本。导入时必须限制文件总量、单图尺寸和解压体积，并阻止绝对路径、`..` 路径穿越及压缩炸弹。
- 多人只同步 `template_id`、模板哈希、结构化字段和时间轴，不同步客户端绝对路径。模板缺失或哈希不同的客户端显示明确占位页。
- 规划字段包括线路、当前站/下一站、到站时间、开门方向、换乘、广告与紧急信息。现实位置、速度和列车状态通过显式授权的数据适配器提供。
- “客户端上传模板到服务器”将是默认关闭的高权限能力，需要管理员授权、哈希校验、大小限制和审核流程，不允许普通网页内容借此执行脚本。
- 当前客户端已支持 `manifest.json` / `display-script.json` 声明式模板、PNG 背景以及独立 HTML 模板编辑器；Rail Map Toolkit 先通过导出 PNG 接入，工程文件和放送机事件等待具有稳定格式与授权边界的专用适配器。

### 三棱柱车厢吊顶显示屏

- 当前提供等边三角形截面的横向延展吊顶显示屏：两块斜面可联动或独立配置，顶部吊装面不作为显示面。
- 每个显示面可选择 VIDEO/WEB/VNC 媒体或结构化交通信息，移动结构中默认不依赖红石供电。
- 后续补充 Create 车厢内对斜面 WEB/VNC 的精确射线交互、端盖款式、车门联动和列车运行数据适配器。

## Create / 机械动力载具兼容

- 当前兼容层以 Create 6.0.9 为最低版本，保证车厢虚拟世界中的普通拼合 Screen 能持续显示 VIDEO 与 WEB，复用原 `tile_id/group_id`、浏览器、解码器和动态纹理。
- Create 已向普通方块实体渲染器提供移动/旋转后的 PoseStack；MineScreen 只在车厢局部坐标中拼合画布，禁止再次施加世界变换。
- 可选 Create API 桥使用 `AbstractContraptionEntity.toGlobalVector` 将画布包围盒、屏幕音源和车厢内相连音箱转换到插值后的世界位置；可见性不再固定在组装地点。
- 移动组以弱引用虚拟世界缓存和短期渲染心跳管理生命周期，防止组装/解组时刷新网页，也防止已卸载车厢遗留浏览器或 FFmpeg 线程。
- 第一层兼容把车厢中的屏幕视为移动时保持通电，因为 Create 的虚拟红石不会像普通世界一样持续 tick；后续提供组装前锁存、始终开启和虚拟红石三种供电策略。
- 当前交互层已将玩家世界射线转换到 `AbstractContraptionEntity` 的局部空间，可在移动中的同平面屏幕上继续使用准星点击、滚轮和手持键盘；Pointer Lock 回中会再转换到车厢实时世界坐标。
- 固定键盘与车厢红石仍暂不支持：固定键盘只解析普通世界连接，移动屏幕暂按保持工作处理。后续再评估车厢方块遮挡射线、固定键盘连接及组装前锁存/虚拟红石模式。
- 后续仍需对组装、车站停靠、跨维度、传送门和解组进行游戏内专项测试。

## 现实世界数据桥接

- 所有外部交互必须显式启用，并按适配器提供 HTTPS REST、WebSocket 与 MQTT 等方式；默认不对外发送任何世界状态。
- 可配置上报维度、展示板 ID、列车/载具状态、站点、速度、朝向以及经过精度削减的位置，用于现实大屏、调度系统或信息发布服务。
- 精确坐标、玩家 UUID/名称、Cookie、密码、VNC 凭据和本地媒体路径默认禁止发送；敏感字段需要逐项授权。
- 支持域名白名单、TLS、令牌/证书认证、消息签名、速率限制、重放保护、离线队列上限和审计日志。
- 外部系统向游戏下发展示内容时，由服务端校验设备、模板、字段与权限；客户端不得把外部输入直接当作无限制 HTML/脚本执行。

## 新设备与便携终端

### 手机

- 可手持的MineScreen手机，提供小尺寸触控屏、网页、媒体遥控和屏幕投送入口。
- 手机可作为主机/屏幕的便携遥控器，也可在获得权限后预览指定 `screen_id`。
- 为多人环境设计独立的本地凭据仓库；不会把Cookie、密码、VNC凭据或本地媒体路径同步到服务器。
- 研究物品模型上的实时低分辨率渲染，并提供帧率、距离和电量/发热风格的性能限制。

### 行车导航仪

- 可放置或安装在载具上的导航终端，显示地图、路线、速度、航向和媒体控制。
- 与常见载具Mod通过可选适配器集成，不强制依赖某一种车辆API。
- 支持触控式大按钮、夜间主题、驾驶期间输入限制以及服务器权限校验。
- 后续研究网页地图、离线地图瓦片与原生Minecraft地图数据之间的切换。

## 屏幕与光照

### 视频驱动光照更新

- 从VIDEO/WEB/VNC画面计算低频平均亮度、主色和边缘亮度，用于屏幕附近的环境光变化。
- 默认只使用降采样亮度网格，不读取或同步完整视频帧。
- 光照更新频率与画面帧率分离，预计限制在1–5Hz，并设置变化阈值避免区块光照频繁重建。
- 优先研究客户端光学/着色器发光，不直接修改服务端方块光照；服务端动态光作为可选高成本模式。
- 为光敏感用户提供亮度上限、色温、闪烁抑制和完全关闭选项。
- 与动态光源、光影包和渲染优化Mod通过可选适配器协作。

## 分发与同步

- 视频点播使用内容哈希、分片缓存和可选P2P交换，游戏服务器只同步manifest与时间轴。
- 实时WEB已提供“权威渲染节点 + JPEG + P2P树形转发”MVP；后续升级为H.264/AV1硬件编码、WebRTC/QUIC、NAT穿透、端到端加密和动态树修复。
- 增加媒体文件大小、时长和内容哈希验证，确保同一 `media_id` 映射到一致文件。
- 改进服务器时钟偏差估计、RTT补偿和小幅播放速率校正，减少视频硬seek。
- P2P不可用时始终保留客户端直连内容源的安全回退。

### NAT 穿透与独立中继（v1.2.0 不实现）

- 后续评估 ICE、STUN、TURN 与 QUIC/WebRTC 连接协商；v1.2.0 现有 P2P 默认关闭，兼容模式也不会自动启用它。
- 研究独立画面中继服务，使 20 Mbps/FRP 游戏服务器不承担网页或 VNC 画面转发；服务端仍只同步状态与时间轴。
- 设计中必须明确提示公网/局域网地址暴露、节点上行消耗、TURN 费用、内容隐私和滥用风险，并支持管理员完全禁用。
- 第三方 STUN/TURN、浏览器、编解码器及下载源须接受许可证、供应链和地区法律审查，不由客户端静默下载。
- 移动端动态 WEB 若未来恢复，优先采用启动器/平台提供的原生 WebView 桥，而不是为每个平台私自打包一套 CEF。
- Android WebView/Chromium 桥需要由 Pojav、FCL、ZL2 等启动器提供受控纹理共享、输入转发与
  生命周期接口；桌面 MCEF/JCEF 二进制不能直接当作 Android Chromium 使用。

## UI与可访问性

- 继续扩展 `MineScreenUiProvider`，允许Otyacraft Engine Renewed等UI框架通过适配器接管外观。
- Cloth Config API适配器用于更完整的配置页面；Architectury API适配器用于跨加载器UI集成层。
- 这些前置并非彼此通用的控件皮肤API，因此采用独立适配模块，不在核心Mod中硬编码其内部类。
- 增加触控布局、键盘导航、屏幕阅读器提示、颜色对比预设和可缩放控件密度。

## VNC

- 在当前Tight支持基础上评估ZRLE、H.264扩展编码和硬件视频解码。
- 增加吞吐量、压缩率、更新区域和解码耗时统计面板。
- 多人观看同一VNC时复用现有P2P树，提供可选“单一RFB上游 + Tight脏矩形/编码矩形直转”模式，减少VNC服务器重复连接和上行；控制事件沿树回传。
- 研究VeNCrypt/TLS，替代经典RFB DES验证和明文传输。
